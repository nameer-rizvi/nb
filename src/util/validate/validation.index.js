const validateValidation = {
  isSanitized: require("./validation.isSanitized"),
  maxLength: require("./validation.maxLength"),
  type: require("./validation.type"),
};

module.exports = validateValidation;
